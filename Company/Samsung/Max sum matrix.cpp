/*
Given an NxM (N rows and M columns) integer matrix with non-negative values (0..MAX_INT inclusive). 
What is the maximum sum from going top left (0, 0) to bottom right (N-1, M-1)? The condition is 
that when you're at the point (p, q), you can only move to either right (p, q+1) or down (p+1, q).
*/

// basic bfs problem solve on free time